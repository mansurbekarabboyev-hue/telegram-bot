services:
  - type: web
    name: telegram-bot
    runtime: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python kino_bot.py"
    envVars:
      - key: PYTHONUNBUFFERED
        value: "1"
